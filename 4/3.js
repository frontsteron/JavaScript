"use strict";

/*
Используя Math.random() необходимо сгенерировать массив, содержащий 5 цифр в 
диапазоне [0, 9].
После создания массива необходимо вывести в консоль следующие значения:
1. Сумму элементов массива
2. Минимальное значение в массиве
3. Новый массив, содержащий индексы сгенерированного выше массива, в которых 
значение равно 3.
Пример: Если у нас сгенерировался массив [2, 3, 5, 7, 3], то мы должны вывести 
в консоль массив [1, 4]. Такой массив получился потому что в сгенерированном
массиве тройки лежат под индексами 1 и 4. Если троек в сгенерированном массиве
не окажется, значит нужно будет вывести пустой массив.
*/

// Функция для генерации случайного целого числа в заданном диапазоне
function getRandomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// Генерация массива с 5 случайными цифрами в диапазоне [0, 9]
let randomArray = [];
for (let i = 0; i < 5; i++) {
    randomArray.push(getRandomInt(0, 9));
}

console.log('Сгенерированный массив:', randomArray);

// 1. Сумма элементов массива
let sum = randomArray.reduce(function (total, currentValue) {
    return total + currentValue;
}, 0);
console.log('Сумма элементов массива:', sum);

// 2. Минимальное значение в массиве
let min = Math.min(...randomArray);
console.log('Минимальное значение в массиве:', min);

// 3. Новый массив с индексами, где значение равно 3
let indexesWithThree = [];
for (let j = 0; j < randomArray.length; j++) {
    if (randomArray[j] === 3) {
        indexesWithThree.push(j);
    }
}
console.log('Массив с индексами, где значение равно 3:', indexesWithThree);

